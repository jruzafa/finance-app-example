<%= form_for(@entry) do |f| %>
  <% if @entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@entry.errors.count, "error") %> prohibited this entry from being saved:</h2>

      <ul>
      <% @entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :concept %><br />
    <%= f.text_field :concept %>
  </div>
  <div class="field">
    <%= f.label :amount %><br />
    <%= f.text_field :amount %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  

 <div class="field">
   
    <%= select("entry_category_id","id", @categories.map{|d| ["#{d.name}",d.id]}) %>
  </div>




  <div class="field">
    <%= f.label :type_id %><br />
    <%= select("entry_category_id","id", @types.map{|d| ["#{d.name}",d.id]}) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
